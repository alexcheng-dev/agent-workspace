name: cache-probe
on:
  workflow_dispatch:
    inputs:
      cache_key:
        description: Cache key
        required: true
        type: string
      size_mb:
        description: Probe file size in MB
        required: false
        default: '256'
        type: string
permissions:
  actions: write
  contents: read
jobs:
  save:
    runs-on: ubuntu-latest
    outputs:
      cache_key: ${{ steps.meta.outputs.cache_key }}
    steps:
      - id: meta
        run: echo "cache_key=${{ inputs.cache_key }}" >> "$GITHUB_OUTPUT"
      - name: Generate payload
        run: |
          set -euo pipefail
          mkdir -p cache-probe
          start=$(date +%s)
          dd if=/dev/urandom of=cache-probe/payload.bin bs=1M count=${{ inputs.size_mb }} status=none
          sha256sum cache-probe/payload.bin | tee cache-probe/payload.sha256
          end=$(date +%s)
          echo "GENERATE_SECONDS=$((end-start))" >> "$GITHUB_STEP_SUMMARY"
      - name: Save cache
        uses: actions/cache/save@v4
        with:
          path: cache-probe
          key: ${{ inputs.cache_key }}
  restore:
    needs: save
    runs-on: ubuntu-latest
    steps:
      - name: Restore cache
        uses: actions/cache/restore@v4
        with:
          path: cache-probe
          key: ${{ needs.save.outputs.cache_key }}
          fail-on-cache-miss: true
      - name: Verify payload
        run: |
          set -euo pipefail
          sha256sum -c cache-probe/payload.sha256
          du -sh cache-probe | tee -a "$GITHUB_STEP_SUMMARY"
